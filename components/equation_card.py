"""Equation card — a reusable block that shows a metric's formula alongside
the numbers that went into it.

Display only. The card renders values it is given; it never computes them.

Not yet implemented.
"""
from __future__ import annotations
