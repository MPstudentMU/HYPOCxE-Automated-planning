# Pilot fixtures

Small, hand-checked pilot exports used as test inputs. Expected values in tests
are computed by hand from the manual, never captured from the code's own
output.

These files must contain no HN and no other identifying data.
